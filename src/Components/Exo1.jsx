import React from 'react';


const Exo1 = (props) => {

    return(
        <div>
            <h1>{props.titre}</h1>
            <button>{props.btn}</button>
        </div>
    )
};

export default Exo1;